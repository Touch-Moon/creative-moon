# Code Edit Rules

- Do not rewrite the entire file for minor CSS/JS changes.
- Provide only the specific code snippets that need to be updated.
- Use comments like `/* ... existing code ... */` to indicate skipped sections.
- If I ask for a fix, output only the diff or the specific CSS rule, not the whole stylesheet.
